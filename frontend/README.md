# Web3 Message Signer — Frontend (React + Vite + TypeScript)

This is the frontend for the Web3 Message Signer & Verifier project.  
It implements the Dynamic.xyz headless email authentication flow, message signing, signature verification UI, and local signing history.

---

## 🚀 Features
- Dynamic.xyz **Headless Email Authentication**
- Embedded Wallet Provider creation
- Sign any custom message using Ethers.js
- Submit `{ message, signature }` to backend
- Display backend verification result
- Local history stored in `localStorage`
- Beautiful UI built with React + Tailwind

---

## 🛠 Tech Stack
- React 18
- TypeScript
- Vite
- TailwindCSS
- Axios
- Ethers.js

---

## 📦 Installation

```sh
cd frontend
npm install
